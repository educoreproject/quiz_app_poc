# Passcode gate — set to the SHA-256 hash of your chosen passcode.
# Generate one with:  node scripts/hash-passcode.mjs "your passcode"
# Leave unset to use the default passcode "educore".
VITE_PASSCODE_SHA256=
